Flask>=3.0,<4.0
# Optional: nicer CPU/RAM stats on the dashboard. Without it, Linux falls back
# to reading /proc directly, so it is not strictly required.
psutil>=5.9
# Optional: the interactive (PTY) terminal at /terminal — real vim/nano/htop/REPL
# over a WebSocket. Without it the page shows install instructions; the rest of
# the app is unaffected. Works with the single-process threaded dev server.
flask-sock>=0.7
